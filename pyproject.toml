[project]
name = "proyecto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.133.1",
    "psycopg[binary,pool]>=3.3.3",
    "psycopg-pool>=3.3.0",
    "uvicorn>=0.41.0",
]
